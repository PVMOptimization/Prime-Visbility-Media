import React from "react";
import GrowthPipeline from "./GrowthPipeline";

// This is the actual "page" — it just renders the pipeline component.
// Point your router at this file (see routing instructions below).
export default function GrowthPipelinePage() {
  return <GrowthPipeline />;
}
